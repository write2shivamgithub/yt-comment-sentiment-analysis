from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A ml project to classify the sentiment of youtube comment',
    author='Shivam',
    license='',
)
